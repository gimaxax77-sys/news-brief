# 인수인계 — 뉴스 브리핑 (2026-08-04)

## 지금 상태: **완성·가동 중**

| | |
|---|---|
| 라이브 | <https://gimaxax77-sys.github.io/news-brief/> |
| 저장소 | `gimaxax77-sys/news-brief` (공개) |
| 자동 실행 | 작업 스케줄러 **`AXdata_NewsBrief`** · **2시간마다 05분**(09:05 시작) |
| 야간 정지 | 한국시간 **01~08시**는 `brief.py` 가 즉시 종료 — 알림도 요약 호출도 없음 |
| 아침 몰아보기 | **09시** 실행이 8시간치를 한 번에 처리하고 핫뉴스 8건을 알림 |
| 알림 봇 | 텔레그램 — 봇 토큰·챗ID 는 로컬 `.env` 에만 둡니다(공개 저장소라 여기 안 적습니다) |
| 로그 | `brief_log.txt` (저장소 제외) |
| 테스트 | `python -X utf8 -u test_brief.py` → **33항목** |

수집 21소스 · 약 240건 유지 · 1회 5초.

## 바로 이어서 할 일

1. **한줄요약 켜기 — Gim 의 Claude API 결제 대기 중.**
   결제가 풀리면 `.env` 의 `ANTHROPIC_API_KEY` 를 채우고, `brief.py` 에서 기사마다
   `g["summary"]` 를 채우는 단계를 넣으면 된다. **화면·알림은 이미 요약을 받을 준비가 돼 있다**
   (`render._기사()` 가 `summary` 가 있으면 `.sum` 으로 그린다).
   - 모델 **Haiku 4.5**(입력 $1 / 출력 $5 per 1M) · 건당 약 $0.0006 · 하루 100건이면 월 약 $1.8.
   - Batches API 는 50% 싸지만 최대 24시간 지연이라 2시간 주기에 안 맞는다. 쓰지 말 것.
2. **며칠 써 본 뒤 소스 정리.** 노이즈가 있으면 `sources.py` 한 줄이다.

## 이 프로젝트에서 밟은 함정 (다시 밟지 말 것)

1. **⛔ 구글뉴스 검색을 좁은 실무 주제에 쓰지 않는다.** 매체명까지 느슨하게 매칭한다.
   `유튜브 크리에이터` → "당진서 논일하던 60대 온열질환 사망"(매체명이 `creatorjournal`).
   → 전용 매체가 있으면 그것을 쓰고, 검색은 전용 소스가 **아예 없는 곳**(Anthropic)에만.
2. **한 소스가 전체를 뒤덮는다.** OpenAI 피드는 아카이브 1,107건을 통째로 준다.
   `PER_SOURCE_MAX`·`MAX_AGE_DAYS` 를 풀지 말 것.
3. **구글뉴스 제목은 `제목 - 매체명`** 이다. 안 떼면 같은 기사가 매체 수만큼 중복된다.
4. **⛔ `.bat` 은 CRLF + ASCII 전용.** LF 로 저장하면 `cmd` 가 줄을 잘못 끊고
   (`set PYTHONIOENCODING` → `THONIOENCODING`), 한글을 넣으면 cmd(cp949)와 파이썬(UTF-8)이
   같은 로그에 섞여 써서 로그가 깨진다. Write 도구로 만들지 말고 바이트를 직접 지정할 것.
5. **일부 RSS 는 문서 뒤에 잡음이 붙어 있다.** 마지막 `</rss>`·`</feed>` 까지 잘라 파싱한다.
6. **검색은 파이썬 테스트로 검증이 안 된다.** 배포 후 브라우저에서 실제로 눌러 볼 것.
   - Pages 배포 지연 → `curl | grep -c 'id="q"'` 로 새 버전인지 먼저 확인.
   - 콘솔 검사 코드는 즉시실행함수로 감쌀 것. 페이지가 `const q` 를 전역에 선언해 두어
     같은 이름을 쓰면 `Identifier 'q' has already been declared` 로 죽는다.

## 다른 프로젝트에 걸린 것

**`axdata_13_auto_upload` — 훅 개선 샘플 v2 가 Gim 검토 대기 중.**
비공개 <https://youtu.be/j6gMMNLG1_8> · <https://youtu.be/8QmSG1UV4-k>
승인되면 대기열 70편에 순차 적용(재렌더 필요, 1편 실측 후 보고). 상세는 그 폴더 `research.md`.
