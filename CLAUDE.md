# 작업 규칙 — axdata_15_news_brief

> **공통 규칙은 상위 `D:\.CODE\AXdata\CLAUDE.md` 한 곳에만 둡니다.**
> 소통 규칙 · 작업 방식 원칙 · 답변/기록 규칙, 그리고 호출 명령(`서브`·`롣`·`멤`)은 그 문서를 따릅니다.
> 이 문서에는 **이 프로젝트에서만 통하는 내용**만 적습니다.

## 이 프로젝트 전용 규칙

1. **테스트 명령** — 망 접속 없이 도는 자체 점검입니다. 코드를 건드렸으면 "완료" 전에 반드시 돌립니다.

   ```
   python -X utf8 -u test_brief.py
   ```

2. **한 번 실행해 보기** — 실제 수집까지 돕니다(망 필요, 과금 없음).

   ```
   python -X utf8 -u brief.py
   ```

3. **소스를 고칠 때는 반드시 접속해서 확인합니다.** RSS 주소는 자주 죽습니다.
   2026-08-04 실측에서 후보 42개 중 21개만 살아 있었고, **Anthropic 은 공식 RSS 가 없습니다**.
   기억으로 목록을 적지 말고 `한소스()` 로 두드려 본 뒤 `sources.py` 에 넣습니다.

4. **⛔ 구글뉴스 검색을 좁은 실무 주제에 쓰지 않습니다.** 구글 뉴스는 매체명까지 느슨하게
   매칭해서, 국내 보도가 드문 주제에서는 관련 없는 기사를 억지로 끌어옵니다
   (`유튜브 크리에이터` → "당진서 논일하던 60대 온열질환 사망", 매체명이 `creatorjournal`).
   **전용 매체가 있으면 그것을 쓰고, 검색은 전용 소스가 아예 없는 곳에만 씁니다.**

5. **`docs/index.html` 은 생성물입니다.** 손으로 고치지 말고 `render.py` 를 고칩니다.
   GitHub Pages 가 이 파일을 그대로 서빙합니다.

6. **`.env` 는 저장소에 없습니다**(gitignore). 키가 비어 있어도 전부 동작합니다 —
   텔레그램 키가 없으면 알림만 안 가고, `ANTHROPIC_API_KEY` 가 없으면 요약만 빠집니다.
