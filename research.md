
---

## 2026-08-04(화) 18~22시 — Claude API 연동 · 요약 · 잡음 걷어내기

### 요청
1. "클로드api 연동해보자 가이드 해줘" → 2. "it뉴스에 it신제품 키워드 추가"
→ 3. "건수를 줄일수있는 키워드 정책 수립해줘" → 4. "기본적으로 내용을 1차 검토하고 요약 하는게 아닌가?"
→ 5. "본문 광고들 없앨수없나"

### 1. Claude API 연동
- **Max 구독은 API 를 포함하지 않는다.** 선불 크레딧을 따로 사야 한다. 이것이 3일간의 병목이었다.
- 콘솔 주소가 `console.anthropic.com` → **`platform.claude.com`** 으로 바뀌어 있었다(실측).
- Gim 이 USD 22.00 결제(18:19), 키 발급(만료 Never). `axdata_13`·`axdata_15` `.env` 에 넣고
  Haiku 4.5 로 실호출 성공($0.00007).

### 2. IT 신제품 소스 추가
- ⛔ **"신제품" 이라는 낱말은 쓰지 않았다.** 국내 뉴스에서 이 말은 식품·생활용품이 압도적이다
  (실측: 우유식빵·주방후드·오메가3·레고·담배).
- 괄호로 IT 품목을 AND 로 묶어도 **구글뉴스 RSS 가 괄호를 안 지킨다**(잡음 23%).
- 세 안을 재어 **품목 이름만 나열**하는 안을 골랐다 — 100건 표본에서 잡음 0건.

### 3. 한줄요약 (`summarize.py` 신설)
- RSS `description` 을 함께 뽑도록 `collect.파싱` 을 고쳤다. 400자 상한.
  - ⛔ 구글뉴스는 이 칸에 **제목을 감싼 링크만** 넣는다 → 본문으로 착각하면 제목을 다시 풀어 쓴
    요약이 나온다. 걸러 낸다(121건).
  - ⛔ Hacker News 는 `Article URL: … Points: 12 # Comments: 3` 만 넣는다. 주소만 지우면
    라벨이 50자쯤 남아 길이 검사를 통과했다 — **이 구멍으로 HN 36건이 매 실행 쓸모없이 요약됐다**(요약분의 24%).
  - ⚠ 길이만 보고 자르면 멀쩡한 한글 설명이 날아간다(한국어는 40자면 온전한 한 문장). **시험이 잡았다.**
- 캐시(`summaries.json`)에 쌓아 재사용. 호출상한 200건/회. 동시 8줄.
- **요약은 덤이다** — 키가 없거나 API 가 죽어도 화면·알림은 그대로 돈다(예외를 밖으로 안 낸다).
- **비용 실측** — 건당 $0.0007. 첫 실행 156건 $0.1102. 신규가 시간당 30~51건이라 **월 약 $7**.
  종전 추정 월 $1.8 은 건수를 하루 100건으로 잡아 틀렸다.

### 4. 건수 줄이기 — 키워드는 문제의 4%였다
| 조치 | 감소 |
|---|---|
| HN 메타데이터 차단(위 버그) | 36 → 4건 |
| 제외어 목록(증시·정치·사건사고·부동산) | 11건 |
→ 요약대상 152 → 109건(28%). ⛔ `정부`·`실적` 같은 **넓은 말은 넣지 않았다** — 시험이 막는다.

### 5. 요약과 같은 호출에서 분야 판정 (Gim 지적)
어차피 본문을 읽는 호출이므로 판정에 드는 값은 출력 두 글자뿐이다. 채점표(애매한 23건 정답표)로 재며 고쳤다.
- **1차 65%** — 틀린 8건이 **전부 「잘못 뺌」**. 「실적·숫자면 제외」 라는 **형식 기준**이
  갤럭시 사전판매·정부 AI 조직 신설·PS5 판매량까지 밀어냈다.
- → 기준을 **「기사가 다루는 대상」 한 축**으로 좁히고 형식은 안 보게 함 + **경계 보기 12개** → **95%**.
- 그래도 사고 — 제목이 `v2.1.221` 뿐인 Claude Code 릴리스와 OpenAI 공식 글이 빠졌다.
  → **주제가 고정된 전용 피드 9곳은 판정 면제**(요약만 받음).
- 역할 분담 — AI 판정은 본문 있는 기사, 제외어는 본문 없는 구글뉴스.

### 6. "본문 광고" 의 정체 = 같은 보도자료 도배
- 먼저 확인 — **우리 페이지에 광고는 0건**(외부 스크립트·프레임·이미지 전무).
- 실측 도배 — 갤럭시 Z8 사전판매 **16건** · 카카오게임즈 MOU **12건** · 오픈AI 해커톤 **9건**.
  제목이 조금씩 달라 지문(제목 해시)으로는 안 잡혔다.
- `collect.한사건씩()` — 제목을 **두 글자 조각**으로 쪼개 겹침 0.30 이상이면 묶는다
  (한국어는 어순·조사가 흔들려 낱말 단위보다 낫다).
- ⛔ 닮음만으로는 **다른 사건이 뭉친다** — `Show HN: cctap`·`Cckeep`·`AxiomCore` 가 하나가 됐다.
  두 겹을 더 걸었다: ① **드문 낱말(고유명사·숫자)을 공유**해야 묶음 ② **Show/Ask HN 은 아예 제외**.
- 대표는 **본문이 있는 쪽**을 우선한다(요약할 수 있는 쪽을 남긴다).
- 결과 **269 → 217건**. 144만 16→2 · 더그림 12→2 · 해커톤 9→4 · Show HN 6→6(보존).

### 확인하지 못한 것
- 크레딧 최소 구매 금액 — 콘솔이 장애 중이라 화면을 못 봤다. $22 = 크레딧 $20 + 부가세 10% 로 **추정**.
- 묶기 문턱 0.30 은 **오늘 표본(269건) 한 벌**로만 조정했다. 며칠 더 보고 조정할 필요가 있다.
- 요약 품질을 Gim 이 아직 며칠 써 보지 않았다. `DESC_MAX`·`PER_SOURCE_MAX` 손잡이는 그대로 뒀다.

### 커밋
`28d0564` IT신제품 소스 · `e3ce0bb` 한줄요약 · `11b787c` 제외어+HN 차단 · `0c7be39` 분야 판정 · `f04eb19` 도배 묶기

---

## 2026-08-05(수) 11~14시 — 앱 로그 사고 조사 · 핫이슈 칸 · 뒤로가기 · 제목 번역

### 요청
1. "어제부터 간헐적으로 메세지를 보낸게 전달이 안되는건지? 씹히는건지? 내용이 없어지는데 확인해줘"
2. "main.log 상한을 늘리면 어떨지? / 핫이슈 탭 만들고 첫번째 탭으로 상단에 추가 / 진입시 바로 첫눈에 핫이슈부터"
3. "각 탭 뉴스더보기 진입하면 이전단계로 돌아가기 버튼이 없고 상단 카테고리 버튼을 눌러야됨 / 우측 하단에 뒤로가기 플로팅 버튼 추가"
4. "영문으로만 되어있는 제목들 한글 번역 한줄 추가"

### 1. 「메시지가 씹힌다」 조사 — **대화로그 쪽 문제가 아니었다**
- 최근 40시간 안에 쓰인 대화로그 **4개 전수 조사** → 사람 입력 334건, **응답 없이 사라진 것 0건.**
- ⚠ **확인 못 한 것** — 앱에서 서버로 가는 길에 사라진 메시지는 로그에 흔적이 없어 **셀 수 없다.**
- 중간에 세운 가설 하나가 틀렸다 — `promptId` 중복을 재전송 흔적으로 봤는데, 그 값은
  **메시지가 아니라 세션 구간마다** 붙는다(한 값이 82건을 공유). 바로 정정하고 다른 각도로 갔다.
- **정황** — 턴 길이 실측: 이 세션 중앙값 **4.3분**·5분 초과 **44%**·최장 23분,
  병행 세션 최장 **74분**. 작업이 도는 중에 보낸 메시지가 가장 위험하다.

### 2. 🐞 진짜 고장 — **Claude 앱 `main.log` 가 20MB에서 멈춰 있었다**
- `%APPDATA%\Claude\logs\main.log` 가 정확히 **20,971,084바이트**에서 **2026-08-04 22:26** 멈춤.
  로테이션도 안 됨(`main1.log` 은 7/31자). 같은 앱의 다른 로그는 오늘도 정상 기록 중.
- 채운 것은 **`Preview not found for server preview-local_...` 17,176줄**, 약 2초 간격,
  **2026-08-02 12:47 시작**. 원인은 **내가 띄운 로컬 프리뷰 서버의 등록을 안 끈 것.**
- **상한은 설정으로 못 바꾼다** — `config.json` 에 로그 관련 키 없음. 늘려도 열흘 뒤 같은 일.
- 권고 — 앱 완전 종료 후 재시작(PC 필요, 세션 끊김) + 꽉 찬 로그 파일 개명.
- ⚠ **인과는 단정하지 않았다.** 시점(어제 22:26)이 Gim 제보와 맞는다는 것까지만 사실이다.
- 재발 방지 — 이후 브라우저 확인 때 `preview_stop` + 포트 사망 + `_preview.html` 삭제를
  **눈으로 확인**하고 보고했다.

### 3. 핫이슈 칸·탭
- 알림에 나간 핫뉴스를 `hot.json` 에 쌓아(12시간·10건) **화면 맨 위 칸**으로 그린다.
  알림과 같은 목록이라 **화면용 모델 호출이 0건**이다.
- 카드에는 요약 대신 **「왜 지금 중요한지」**. 원래 분야 칸에도 **남긴다**(빼면 분야 탭에서 사라짐).
- ⚠ 탭이 다섯이 되어 360px 한 줄에 안 들어감 → **탭에서만** 이름 축약(AI·클로드→AI, 유튜브·쇼츠→유튜브).
  `data-val` 은 원래 이름이라 필터·색·섹션 제목은 그대로.
- **브라우저 360x800 실측** — 마지막 탭 오른쪽 끝 334px(여유 26px)·줄바꿈 없음·
  첫 화면 6장 중 3장이 핫이슈·핫이슈 탭 누르면 5건.

### 4. 뒤로가기 플로팅 단추
- 분야 선택·검색 중일 때만 뜬다. 누르면 **분야와 검색어를 함께** 풀고 맨 위로.
  하나만 풀면 화면이 걸러진 채라 안 돌아간 것처럼 보인다.
- 실측 — 101x45px(처음 43px 이라 터치 권장 44px 미달 → padding 1px 상향),
  맨 아래로 굴려도 마지막 카드(711px)를 안 가림(단추 741px).
- **안 한 것** — 폰 뒤로가기 제스처 연동. 잘못하면 페이지에 갇힌다.

### 5. 영문 제목 한국어 한 줄
- **요약이 붙은 카드에는 안 단다** — 이미 한국어 한 줄이 있다. 영문 95장 중 **61장이 여기 해당**,
  34장만 대상이었다. 결과: 한글이 한 줄도 없는 카드 **0장**.
- 🐞 **첫 실행에서 25건이 통째로 버려졌다.** 입력을 `1. 제목` 으로 번호 매겨 보내니 모델이
  그 형식을 따라 `1. 번역` 으로 답했는데, 파싱이 `번호|글` 만 받고 있었다. **토큰은 다 쓰고
  5건만 붙었다.** 구분자를 넓게 받도록 고치고(`[|.):\-]`) 시험이 네 형식을 확인한다.
- 발견 경위 — 로그에 **「새로 N건」과 「$비용」을 나란히** 찍어 뒀기에 어긋남이 눈에 띄었다.

### 확인하지 못한 것
- 앱 로그 루프가 메시지 유실의 **원인인지** — 증명 못 함. 앱 재시작 뒤 재발 여부로만 알 수 있다.
- 묶기 문턱 0.30 은 여전히 하루치 표본으로만 조정된 상태다.
- 아침 09시 알림의 실제 품질 — 오늘 09:05 이 첫 실행이었고 6건을 골랐다(내용 검토는 Gim 몫).

### 커밋
`f7b9f29` 핫이슈 칸·탭 · `215be67` 뒤로가기 단추 · `a67353a` 제목 번역
